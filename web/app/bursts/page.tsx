import { ScannerView } from "../Scanner";

/* Named for what it lists rather than for the machine that fills it. "Scanner" is the
 * thing in the back; bursts are what the operator came to see, and the rail already
 * has a "Daily Scanner" entry pointing somewhere else. */
export default function BurstsPage() {
  return <ScannerView />;
}
